import { DisabledControlConditionallyAddComponent } from "src/assets/examples/decorators/disabledControlConditionally/add/disabledControlConditionally-add.component";

export const DISABLED_CONTROL_CONDITIONALLY_COMPONENT_EXAMPLE_CONSTANT: { [key: string]: any } = {
    decorators:
    {
        add: DisabledControlConditionallyAddComponent
    }
}