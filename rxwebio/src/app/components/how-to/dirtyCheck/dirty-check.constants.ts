import { DirtyCheckComponent } from "src/assets/examples/how-to/dirtyCheck/complete/dirty-check.component";

export const DIRTY_CHECK_COMPONENT_EXAMPLE_CONSTANT: { [key: string]: any } = {
    validators:
    {
        complete:DirtyCheckComponent,
        
    }
}