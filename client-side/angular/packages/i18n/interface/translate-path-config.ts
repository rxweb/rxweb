import { TranslateGenerateConfig } from "./translate-generate-config";

export interface TranslatePathConfig {
    optimizeJson: string;
    i18n: string;
    component: string;
    generate: TranslateGenerateConfig;
}
